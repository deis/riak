apiVersion: v1
kind: Service
metadata:
  name: deis-riak-cs
  namespace: deis
  labels:
    heritage: deis
spec:
  ports:
    - port: 8081
      targetPort: 8081
      name: http
      protocol: TCP
  selector:
    name: riak-stanchion
